/**
 * @module
 *
 * This module previously contained the voxel gun-sprite projector
 * (`projectVoxelGunSprite` and `ProjectedGunVoxel`). It has been removed in
 * slice 04c-impl-renderer and replaced by the palette-indexed 2D sprite
 * decoder in {@link gun-sprite-decode.ts}. See the plan (No Deferred Cleanup
 * Policy) for rationale.
 */
