/**
 * Type definitions for the framebuffer module extracted from
 * {@link module:./framebuffer}.
 *
 * @module
 */

/**
 * Resolved dimensions of a flat RGBA framebuffer.
 */
export interface NeatensteinFramebufferSize {
  /** Width in pixels. */
  width: number;
  /** Height in pixels. */
  height: number;
}
