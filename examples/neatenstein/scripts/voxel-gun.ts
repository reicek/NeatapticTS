/**
 * @module
 *
 * This module previously contained the procedural voxel gun descriptor
 * (`buildVoxelGun`). It has been removed in slice 04c-impl-renderer and
 * replaced by the palette-indexed 2D sprite asset in
 * `examples/neatenstein/gun-sprite-data.js` decoded via
 * {@link gun-sprite-decode.ts}. See the plan (No Deferred Cleanup Policy) for
 * rationale.
 */
