/**
 * Flat compatibility facade for the `Group` chapter.
 *
 * The heavier graph-composition behavior lives in `group/`, while this root
 * file preserves the traditional architecture import surface.
 */
export { default } from './group/group';
