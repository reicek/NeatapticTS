/**
 * Flat compatibility facade for the `Layer` composition chapter.
 *
 * Use the `layer/` folder when you want the implementation details and helper
 * surfaces; use this root file when you want the stable public import path.
 */
export { default } from './layer/layer';
