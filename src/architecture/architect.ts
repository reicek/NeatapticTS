/**
 * Flat compatibility facade for the architecture builder presets.
 *
 * The implementation lives under `architect/`, where the preset builders and
 * their supporting helpers can stay chapter-scoped without forcing callers to
 * change long-standing imports.
 */
export { default } from './architect/architect';
