{
  "tools": [
    {
      "name": "devtools",
      "description": "Lazy-load router to the real devtools MCP server. Forward { operation, args? } and the facade will lazily spawn the real server on first use.",
      "inputSchema": {
        "type": "object",
        "required": ["operation"],
        "properties": {
          "operation": {
            "type": "string",
            "description": "Real devtools operation name (for example search_corpus or take_screenshot)."
          },
          "args": {
            "type": ["object", "null"],
            "description": "Arguments object for the real operation; omit when the operation takes no arguments."
          }
        }
      }
    }
  ]
}
